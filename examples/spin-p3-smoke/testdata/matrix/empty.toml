spin_manifest_version = 2

[application]
name = "picoclaw-spin-outbound-matrix-empty"
version = "0.1.0"

[[trigger.http]]
route = "/..."
component = "probe"

[component.probe]
source = "../../matrix.wasm"
allowed_outbound_hosts = []
